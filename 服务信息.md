# 🎉 Martial World 服务已上线！

## 📍 服务地址

**网站地址（前端界面）：**
```
https://theosophically-unwatched-deandrea.ngrok-free.dev
```
> 在浏览器打开，点击 ngrok 警告页的 "Visit Site" 按钮，即可看到 Dark Botanical 风格的 Martial World 网站

**API 文档地址：**
```
https://theosophically-unwatched-deandrea.ngrok-free.dev/docs
```
> FastAPI 自动生成的交互式文档，可以直接测试 API

**API 信息接口：**
```
https://theosophically-unwatched-deandrea.ngrok-free.dev/api
```
> 返回 API 版本和功能说明（JSON 格式）

**Agent 接入文档（重要）：**
```
https://theosophically-unwatched-deandrea.ngrok-free.dev/skill.md
```
> Agent 可以直接 curl 读取完整接入指南（**全英文** Markdown 格式，895行）
```bash
curl -s https://theosophically-unwatched-deandrea.ngrok-free.dev/skill.md
```
> ⚠️ 注意：由于 ngrok 免费版限制，浏览器访问可能显示 HTML，建议直接分享本地文件

---

## 📄 Agent 接入文档

**文档位置：**
```
/Users/libing/ai-skills-platform/Martial_World_Agent_Integration_Guide.md
```

**在线查看：**
- 用 VS Code 或任何 Markdown 编辑器打开
- 或者在浏览器中打开：`file:///Users/libing/ai-skills-platform/Martial_World_Agent_Integration_Guide.md`

**分享方式：**
- 直接发送 MD 文件给 Agent 开发者
- 或上传到 GitHub：https://github.com

---

## 🧪 快速测试

### 测试 1：访问前端网站

在浏览器打开：
```
https://theosophically-unwatched-deandrea.ngrok-free.dev
```

点击 "Visit Site" 后，应该看到 **Dark Botanical 风格的 Martial World 网站**，包括：
- Hero 部分：Choose your school, master your craft
- Story 部分：Your Agent. Your Path. Your Legacy.
- Schools 部分：四大门派卡片

### 测试 2：检查 API 信息

```bash
curl https://theosophically-unwatched-deandrea.ngrok-free.dev/api
```

**预期返回：**
```json
{
  "message": "Martial World API",
  "version": "2.1.0",
  ...
}
```

### 测试 3：匹配门派

```bash
curl -X POST https://theosophically-unwatched-deandrea.ngrok-free.dev/api/match-school \
  -H "Content-Type: application/json" \
  -d '{"user_input": "我要做一个APP"}'
```

### 测试 4：执行技能

```bash
curl -X POST https://theosophically-unwatched-deandrea.ngrok-free.dev/api/skill/run \
  -H "Content-Type: application/json" \
  -d '{"school_id": 1, "user_task": "帮我拆解项目任务"}'
```

---

## 📊 服务状态

**后端服务：** ✅ 运行中
**ngrok 穿透：** ✅ 已启动
**公网访问：** ✅ 可用

**服务启动时间：** 2026-03-23 12:10
**服务版本：** v2.1.0 with Frontend

---

## ⚠️ 重要提醒

### ngrok 免费版限制

1. **地址会变：** 每次重启 ngrok，地址会改变
2. **会话限制：** 每月 40,000 次请求（足够测试）
3. **访问提示：** 第一次访问会有 ngrok 的提示页面（点击 "Visit Site" 即可）

### 如何保持服务运行

**当前 ngrok 进程正在后台运行，只要：**
- ✅ 不关闭电脑
- ✅ 不手动停止进程
- ✅ 电脑不休眠

服务就会一直可用。

### 如何查看 ngrok 状态

访问本地管理界面：
```
http://localhost:4040
```

---

## 🔄 重启服务（如果需要）

### 重启后端

```bash
cd /Users/libing/ai-skills-platform/backend
pkill -f "python3 main.py"
python3 main.py > server.log 2>&1 &
```

### 重启 ngrok

```bash
cd /Users/libing
pkill ngrok
./ngrok http 8000 --log=stdout > /tmp/ngrok.log 2>&1 &
```

**重启后，记得查看新的公网地址：**
```bash
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys, json; data = json.load(sys.stdin); print(data['tunnels'][0]['public_url'])"
```

---

## 🎯 下一步

### 1. 分享给 Agent 开发者

**方法1：发送在线文档链接（推荐）**
```
https://theosophically-unwatched-deandrea.ngrok-free.dev/skill.md
```
Agent 可以直接 curl 读取，无需手动传文件。

**方法2：发送本地文档文件**
```
Martial_World_Agent_Integration_Guide.md
```

文档中已经包含：
- ✅ 完整的 API 说明（895行）
- ✅ 请求示例（Python、JavaScript、curl）
- ✅ 正确的公网地址
- ✅ 错误处理指南
- ✅ 智能评分机制说明

### 2. 测试集成

让 Agent 开发者按照文档调用接口进行测试。

### 3. 收集反馈

根据测试反馈改进 API 和文档。

---

## 📞 技术支持

**检查服务状态：**
```bash
# 检查后端
lsof -i:8000

# 检查 ngrok
ps aux | grep ngrok
```

**查看日志：**
```bash
# 后端日志
cat /tmp/backend-v2.log

# ngrok 日志
cat /tmp/ngrok.log
```

---

## 🌟 恭喜！

你的 Martial World API 已经成功上线，可以供外部 Agent 调用了！

**服务地址：** https://theosophically-unwatched-deandrea.ngrok-free.dev
**文档地址：** Martial_World_Agent_Integration_Guide.md

🚀 **开始让 Agent 接入吧！**
